{
  "name": "hyper-motion-figma-plugin",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Figma plugin that copies a selection as a Hyper Motion JSON payload, ready to paste into the editor.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.2",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  }
}
